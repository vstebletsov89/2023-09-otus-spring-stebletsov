spring:
  datasource:
    url: jdbc:h2:mem:testdb
  application:
    name: "Homework #5 (test env)"
  shell:
    interactive:
      enabled: false
  flyway:
    enabled: true
    locations: classpath:db/tests