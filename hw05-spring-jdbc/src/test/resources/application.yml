spring:
  datasource:
    url: jdbc:h2:mem:testdb
  sql:
    init:
      mode: always
      data-locations: data.sql
      schema-locations: schema.sql
  application:
    name: "Homework #5 (test env)"
  shell:
    interactive:
      enabled: false